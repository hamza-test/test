--------------------git---------------------
git is responsible to keep changing in your sourc code.when ever we done change in code we push it to the master branch. thats y git is oued to keep tracking of changing.

----------------git hub-----------------------
git hub is a company that provides the git repositry hosting.centralized place where you can share your ode with others.

-----------------git branch-------------------
git branch( show prasent branch)
git branch -a (show all local branch and remote branches as well)
git branch -r (show all remomote branches)
git checkout(switches between branches)
	git checkout branch-name
	git checkout -b new_branch_name( create new branch)
git clone(copy a remote repositry to local machine.)
git add (used to add new lines of code in branch)
git commit(used to savee all changes to the local repositry)
git push( sends commits from local repositry to remote repositry)
git pull(update local repositry from remote)
git merge(kisi branch pe kam kr rahy hon tu usko kisi branch k 7 add krne k liye merge use krty) integrate change in two branches.
git stash( before comminting ne changes we store snapshoot of our changes.)
git rebase (update the branch with latest marged code is rebasing.)
